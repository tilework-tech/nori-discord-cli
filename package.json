{
  "name": "nori-discord-cli",
  "version": "0.1.0",
  "description": "CLI for interacting with the Discord REST API, designed for coding agents",
  "type": "module",
  "bin": {
    "nori-discord": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/index.js",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  }
}
